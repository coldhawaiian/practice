/*
 * Specifying `main` method for program that doesn't extend `App` trait:
 *
 * object HelloWorld {
 *   def main(arg: Array[String]) = {
 *     println("Hello World!")
 *   }
 * }
 */
 object HelloWorld extends App {
   println("Hello World!")
 }
